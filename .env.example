# =============================================================================
# BookCode Environment Variables
# =============================================================================
# Copy this file to .env and fill in the values for local development.
# For Cloud Run deployment, set these as environment variables in the service.
# =============================================================================

# -----------------------------------------------------------------------------
# DATABASE (Required)
# -----------------------------------------------------------------------------
# Option 1: Individual database connection variables (recommended for Cloud Run)
# These take priority over DATABASE_URL if all are set
DB_HOST="10.2.0.5"
DB_USER="postgres"
DB_PASSWORD="your-password"
DB_NAME="bookcode"
# DB_PORT="5432"  # Optional, defaults to 5432

# Option 2: PostgreSQL connection string (used if DB_* vars are not set)
# Local: postgresql://user:password@localhost:5432/dbname
# Cloud SQL: postgresql://user:password@/dbname?host=/cloudsql/PROJECT:REGION:INSTANCE
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/bookcode"

# -----------------------------------------------------------------------------
# AUTHENTICATION (Required)
# -----------------------------------------------------------------------------
# NextAuth.js secret - used to encrypt session tokens
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-super-secret-key-change-in-production"

# Full URL of your application (include https:// in production)
# Cloud Run: https://your-service-abc123-uc.a.run.app
NEXTAUTH_URL="http://localhost:3000"

# Admin password for dashboard access
# Use a strong password in production
ADMIN_PASSWORD="your-secure-admin-password"

# -----------------------------------------------------------------------------
# GOOGLE CLOUD (Optional - for production image storage)
# -----------------------------------------------------------------------------
# If not set, images are stored as base64 data URLs in the database
# For production, configure these for Cloud Storage

# Your GCP project ID
GOOGLE_CLOUD_PROJECT=""

# Cloud Storage bucket name for wave code images
# Bucket should have public read access or use signed URLs
GCS_BUCKET_NAME=""

# -----------------------------------------------------------------------------
# CORS (Optional)
# -----------------------------------------------------------------------------
# Comma-separated list of allowed origins for the public API
# Set this to your companion app's domain
# Default: * (all origins allowed)
ALLOWED_ORIGINS="http://localhost:3000,https://your-app.com"

# -----------------------------------------------------------------------------
# CLOUD RUN SPECIFIC (Set automatically by Cloud Run)
# -----------------------------------------------------------------------------
# PORT - Cloud Run sets this automatically (default: 8080)
# K_SERVICE - Service name
# K_REVISION - Revision name
# K_CONFIGURATION - Configuration name
